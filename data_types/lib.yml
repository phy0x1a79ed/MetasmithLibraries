types:
  hierarchical_clustering.py:
    properties:
      src: hierarchical_clustering
      usage: library
  local:
    properties:
      src: local
      usage: library
  response_surface.py:
    properties:
      src: response_surface
      usage: library
  pangenome_heatmap.py:
    properties:
      src: pangenome_heatmap
      usage: command line
  extract_noncoding_chunks.py:
    properties:
      src: extract_noncoding_chunks
      usage: command line
  merge_promotech_results.py:
    properties:
      src: merge_promotech_results
      usage: command line
  fabfos_evidence.py:
    properties:
      src: fabfos_evidence
      usage: command line
  fabfos_embed_transfer.py:
    properties:
      src: fabfos_embed_transfer
      usage: command line
  # ECSPr network engine. Vendored as a unit: ecspr_build imports ecspr_graph at
  # module top, which imports ecspr_directed at module top. Splitting them or
  # reaching into another worktree with sys.path.insert is what made the tier-3
  # build unreproducible.
  ecspr_build.py:
    properties:
      src: ecspr_build
      usage: command line
  ecspr_graph.py:
    properties:
      src: ecspr_graph
      usage: library
  ecspr_directed.py:
    properties:
      src: ecspr_directed
      usage: library
